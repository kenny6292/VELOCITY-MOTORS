"use client"; import {FormEvent,useState} from "react"; import Link from "next/link"; import {createClient} from "../../lib/supabase/browser"; export default function Forgot(){const [email,setEmail]=useState("");const [message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();const {error}=await createClient().auth.resetPasswordForEmail(email,{redirectTo:location.origin+"/reset-password"});setMessage(error?.message||"If an account exists for that email, a reset link has been sent.");}return <main className="container" style={{maxWidth:520,padding:"80px 0"}}><h1>Reset your password.</h1><form onSubmit={submit} className="glass" style={{padding:24,borderRadius:12,display:"grid",gap:14}}><input required type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} style={input}/><button className="btn btn-primary">Send reset link</button>{message&&<p className="muted">{message}</p>}<Link className="muted" href="/login">← Back to sign in</Link></form></main>}const input={background:"#08090b",border:"1px solid #2a2f38",color:"#fff",padding:"13px",borderRadius:7};