import{NextResponse}from"next/server";import{createClient}from"../../../../lib/supabase/server";import{verifyPaystackPayment}from"../../../../lib/paystack";export async function GET(req:Request){const reference=new URL(req.url).searchParams.get("reference");if(!reference)return NextResponse.json({error:"Reference required"},{status:400});try{const result=await verifyPaystackPayment(reference);const sb=await createClient();const status=result.data?.status==="success"?"success":"failed";await sb.from("payments").update({status,paid_at:status==="success"?new Date().toISOString():null}).eq("reference",reference);return NextResponse.redirect(new URL(`/account?payment=${status}`,req.url))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Verification failed"},{status:500})}}