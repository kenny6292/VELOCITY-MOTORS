import{NextResponse}from"next/server";import{createClient}from"../../../lib/supabase/server";export async function POST(req:Request){try{const body=await req.json();const full_name=String(body.full_name||"").trim(),email=String(body.email||"").trim(),phone=String(body.phone||"").trim(),message=String(body.message||"").trim();if(!full_name||!email||!message)return NextResponse.json({error:"Name, email and message are required"},{status:400});const sb=await createClient();const{data:{user}}=await sb.auth.getUser();const{error}=await sb.from("leads").insert({customer_id:user?.id||null,source:"contact",full_name,email,phone,message});if(error)throw error;return NextResponse.json({success:true})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Unable to submit enquiry"},{status:500})}}