"use client"; import {FormEvent,useState} from "react"; import {createClient} from "../../lib/supabase/browser"; export default function Reset(){const [password,setPassword]=useState("");const [message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();const {error}=await createClient().auth.updateUser({password});setMessage(error?.message||"Password updated. You can now sign in.");}return <main className="container" style={{maxWidth:520,padding:"80px 0"}}><h1>Choose a new password.</h1><form onSubmit={submit} className="glass" style={{padding:24,borderRadius:12,display:"grid",gap:14}}><input required minLength={8} type="password" placeholder="New password" value={password} onChange={e=>setPassword(e.target.value)} style={input}/><button className="btn btn-primary">Update password</button>{message&&<p className="muted">{message}</p>}</form></main>}const input={background:"#08090b",border:"1px solid #2a2f38",color:"#fff",padding:"13px",borderRadius:7};