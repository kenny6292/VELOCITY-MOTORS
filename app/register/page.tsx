"use client"; import {FormEvent,useState} from "react"; import Link from "next/link"; import {createClient} from "../../lib/supabase/browser"; export default function Register(){const [name,setName]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [message,setMessage]=useState("");const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);const sb=createClient();const {data,error}=await sb.auth.signUp({email,password,options:{data:{full_name:name}}});if(error)setMessage(error.message);else setMessage(data.session?"Account created.":"Account created. Check your email to confirm your address.");setLoading(false)}return <main className="container" style={{maxWidth:520,padding:"80px 0"}}><p className="muted">JOIN VELOCITY MOTORS</p><h1 style={{fontSize:46}}>Create your account.</h1><form onSubmit={submit} className="glass" style={{padding:24,borderRadius:12,display:"grid",gap:14}}><input required placeholder="Full name" value={name} onChange={e=>setName(e.target.value)} style={input}/><input required type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} style={input}/><input required minLength={8} type="password" placeholder="Password (8+ characters)" value={password} onChange={e=>setPassword(e.target.value)} style={input}/>{message&&<p className="muted">{message}</p>}<button disabled={loading} className="btn btn-primary">{loading?"Creating…":"Create account"}</button><p className="muted">Already registered? <Link href="/login">Sign in</Link></p></form></main>}const input={background:"#08090b",border:"1px solid #2a2f38",color:"#fff",padding:"13px",borderRadius:7};